# telebotttt
new tele bot

## Setup

Install project dependencies:

```bash
pip install -r requirements.txt
```

Set `TELEGRAM_ALLOWED_CHAT_IDS` to a comma-separated list of allowed Telegram chat IDs.
If it is empty or unset, the bot will refuse to operate in all groups. The configured admin can still use it in private DM.
